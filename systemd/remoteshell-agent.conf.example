# Configuration de l'agent RemoteShell
# Copiez ce fichier vers /etc/remoteshell/agent.conf et modifiez les valeurs
# Ce fichier peut être chargé via EnvironmentFile dans le service systemd

# Adresse du serveur RemoteShell
REMOTESHELL_SERVER_HOST=10.0.0.59

# Port du serveur RemoteShell
REMOTESHELL_SERVER_PORT=8081

# ID unique de l'agent (identifiant de la machine)
REMOTESHELL_AGENT_ID=serveur-impression-01

# Nom convivial de l'agent (affiché dans l'interface)
REMOTESHELL_AGENT_NAME="Serveur d'impression principal"

# Token d'authentification (fourni par l'administrateur)
REMOTESHELL_AUTH_TOKEN=test-token

# Options avancées (optionnelles)
# REMOTESHELL_SERVER_TLS=false
# REMOTESHELL_HEARTBEAT_INTERVAL=30s
# REMOTESHELL_RECONNECT_DELAY=10s
# REMOTESHELL_CHUNK_SIZE=65536
# REMOTESHELL_LOG_LEVEL=info
# REMOTESHELL_LOG_FILE=/var/log/remoteshell-agent.log

